services:
  api:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./deployment:/app
    environment:
      - AWS_REGION=${AWS_REGION:-us-east-1}
      # Fallback local para cuando SSM no está disponible
      - WANDB_USER=${WANDB_USER:-}
      - WANDB_API_KEY=${WANDB_API_KEY:-}
